import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import App from "./App";

// Start here tomorrow morning - you're throwing an error indicating that your <Link>
// tag is being rendered outside of either a <BrowserRouter> or a <MemoryRouter>
// Plus, not sure if the below mock is even set up right.
// Tackle this first thing in the morning with fresh eyes
